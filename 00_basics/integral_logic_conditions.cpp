import <print>;

using namespace std;

int main(){

    int condition {-5};

    bool bool_condition = condition;

    if (condition){
       println("We have a {} in our variable", condition);
    } else {
       println("We have a {} in our variable", condition);
    }

    if (bool_condition){
       println("We have a {} in our variable", bool_condition);
    } else {
       println("We have a {} in our variable", bool_condition);
    }
    
    return 0;
}
